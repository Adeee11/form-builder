import styled from "styled-components";

const Form = styled.form`
    display: flex;
    flex-direction: column;
`
const RadioButtonContainer = styled.div`
    
`

export {Form, RadioButtonContainer}